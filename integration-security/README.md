# Integration security technical primitives

This Core area contains stateless OAuth transport contracts, Webhook destination
validation and transport contracts, and Webhook secret cryptography. Tenant
bindings, machine identities, Webhook persistence, delivery lifecycle, routes,
and module permissions are owned by the Application `official.integration`
module. Core has no dependency on that module.
